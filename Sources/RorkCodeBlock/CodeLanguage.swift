import RorkHighlighter

/// Identifies the language rendered by a code block.
///
/// The type is shared with Rork Highlighter, so bundled identifiers such as
/// `CodeLanguage.swift` remain available without another conversion layer.
public typealias CodeLanguage = LanguageID
